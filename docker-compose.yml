services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - .:/workspace
    working_dir: /workspace/backend
    ports:
      - "8000:8000"
    command: sleep infinity
